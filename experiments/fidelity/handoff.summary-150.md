Auth middleware extraction from `web/server.go` into `internal/auth/` package is in progress (2026-04-25, Claude Sonnet 4.6). Files touched: `web/server.go`, `internal/auth/middleware.go`, `internal/auth/middleware_test.go`, `web/server_test.go`. Decisions: legacy session-cookie path preserved (deferred/out of scope); `AuthCheck` renamed to `RequireAuth`; rate limiter not refactored (out of scope). Extraction is safe to merge; tests pass locally; CI failure is an unrelated kane-cve fetch timeout. Next session: investigate `web/server_test.go` flake first (possible real ordering bug or test race). Open questions: delete legacy session-cookie path or keep under a build tag?